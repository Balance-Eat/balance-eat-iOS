name: Beta Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: macos-latest
    timeout-minutes: 90

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1'
          bundler-cache: true

      - name: Configure Git Credentials
        env:
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
        run: |
          DECODED=$(echo "$MATCH_GIT_BASIC_AUTHORIZATION" | base64 --decode)
          USERNAME=$(echo "$DECODED" | cut -d: -f1)
          TOKEN=$(echo "$DECODED" | cut -d: -f2-)
          echo "machine github.com login ${USERNAME} password ${TOKEN}" > ~/.netrc
          chmod 600 ~/.netrc

      - name: Write API Key
        env:
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
        run: |
          printf '%s' "$APP_STORE_CONNECT_API_KEY_CONTENT" > /tmp/AuthKey.p8
          python3 -c "
          import re
          key = open('/tmp/AuthKey.p8').read().strip()
          if '\n' not in key:
              m = re.match(r'(-----BEGIN [^-]+-----)(.*)(-----END [^-]+-----)', key)
              if m:
                  header, body, footer = m.group(1), m.group(2), m.group(3)
                  folded = '\n'.join(body[i:i+64] for i in range(0, len(body), 64))
                  pem = header + '\n' + folded + '\n' + footer + '\n'
                  open('/tmp/AuthKey.p8', 'w').write(pem)
                  print('Reconstructed PEM: ' + str(pem.count(chr(10))) + ' lines')
              else:
                  print('ERROR: cannot parse key')
          else:
              print('Key already has newlines')
          "

      - name: Patch fastlane for OpenSSL 3.x compatibility
        run: |
          TOKEN_FILE="vendor/bundle/ruby/3.1.0/gems/fastlane-2.232.2/spaceship/lib/spaceship/connect_api/token.rb"
          if [ -f "$TOKEN_FILE" ]; then
            ruby -e "
              f = '$TOKEN_FILE'
              c = File.read(f)
              patched = c.gsub('OpenSSL::PKey::EC.new', 'OpenSSL::PKey.read')
              File.write(f, patched)
              puts 'Changed: ' + (c != patched).to_s
            "
            grep -n 'OpenSSL::PKey' "$TOKEN_FILE"
          else
            echo "File not found: $TOKEN_FILE"
            find vendor/bundle -name "token.rb" 2>/dev/null
          fi

      - name: Cache Derived Data
        uses: actions/cache@v4
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-derived-data-${{ hashFiles('**/*.xcodeproj/project.pbxproj') }}
          restore-keys: |
            ${{ runner.os }}-derived-data-

      - name: Deploy to TestFlight
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_GIT_BASIC_AUTHORIZATION: ${{ secrets.MATCH_GIT_BASIC_AUTHORIZATION }}
          APP_STORE_CONNECT_API_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ID }}
          APP_STORE_CONNECT_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_CONTENT: ${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}
          FASTLANE_XCODEBUILD_SETTINGS_TIMEOUT: 120
          FASTLANE_XCODEBUILD_SETTINGS_RETRIES: 3
        run: bundle exec fastlane beta
